You thought right, learning algorithms is the procedure that is run on data. The model represent what was learn by the algorithm.
But the hypermarameters are used to control the learning algorihm


sig eff = tp/tp+fp
recall = tp/tp+fn

`git branch -a -v`
`git switch main`
`git pull upstream main`
`git switch MyBranch`
`git checkout main Exercises/01_Classification.ipynb`
