when BIOS starts: hit F12 and then select the USB to boot from there.

reception room computer:
mac address 4C:D7:17:AA:A5:2E


To get on the network:
had to copy the /etc/resolv.conf file from labc1.fnal.gov

[labc1-fnal-gov Nov22 16:29:40 ~]$ cat /etc/resolv.conf
# Generated by NetworkManager
nameserver 131.225.8.120
nameserver 131.225.17.150


remove password for agrummer:
`sudo passwd -d agrummer`

used this executable to submit key:
`/usr/sbin/makehostkeys`



pretty hostname:
cat /etc/machine-info
static hostname:
cat /etc/hostname

command that checks the system info:
hostnamectl

Transient hostname: set by the DHCP server (https://phoenixnap.com/kb/pretty-hostname)

Static hostname: The standard hostname set by the user.
Transient hostname: A dynamic hostname set by the system kernel. By default, it is the same as the static hostname, but DHCP and mDNS servers can change it on runtime.
Pretty hostname: A free-form hostname defined by the user, usually to describe the system to other users within the network.



labc1 ip:
131.225.179.203

for labc6 needed:
Register/Unregister/Modify Static FQDN/IP address pairs on a single system
from services now


register fixed ip using the

set:
sshd_config

public key auth no
password auth no

restart sshd with systemctl


etc/ keytab

# reinstall:

when booting from usb drive
options:

Install source: add CERN repo https
software selection: CERN recommended

custom partition:
boot and boot/efi both 1GB
swap 32 GB
for / use 256
home gets the rest


on reboot after install : change two selection:
select No - i'll update myself
and instead of auto system updates - just check then

allow time for "finish configuration"


nstall fermilab alma

network >> ipv4 >>  manual
set address, netmask, gatway


Primary Name Server and Secondary Name Server

change /etc/ssh/sshd_config
(password and pubky ath no)
systemctl restart sshd


match the groups for lpc
groupadd -g 5063 us_cms
useradd -g us_cms -u 58219 -d /home/agrummer agrummer

history in "afterInstall_history.md"
mirror in google search fermilab alma



The key for the kerberos principal is in:
/etc/krb5.keytab
if you need to reinstall OS - can copy this file without requesting new kerberos principle
but remove copies when done



# LABC2

08:BF:B8:A1:C7:29

- need to register the computer to get a hostname and static IP
- No Disks appear in the "installation destination".






